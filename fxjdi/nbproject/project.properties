annotation.processing.enabled=true
annotation.processing.enabled.in.editor=false
annotation.processing.run.all.processors=true
application.title=fxjdi
application.vendor=sundar
build.classes.dir=${build.dir}/classes
build.classes.excludes=**/*.java,**/*.form
# This directory is removed when the project is cleaned:
build.dir=build
build.generated.dir=${build.dir}/generated
build.generated.sources.dir=${build.dir}/generated-sources
# Only compile against the classpath explicitly listed here:
build.sysclasspath=ignore
build.test.classes.dir=${build.dir}/test/classes
build.test.results.dir=${build.dir}/test/results
# Uncomment to specify the preferred debugger connection transport:
#debug.transport=dt_socket
debug.classpath=\
    ${run.classpath}
debug.test.classpath=\
    ${run.test.classpath}
# This directory is removed when the project is cleaned:
dist.dir=dist
dist.jar=${dist.dir}/fxjdi.jar
dist.javadoc.dir=${dist.dir}/javadoc
endorsed.classpath=
excludes=
file.reference.junit.jar=/kalyan/work/sun2010/JfxCompiler/jars/junit.jar
file.reference.tools.jar=${java.home}/../lib/tools.jar
includes=**
jar.compress=false
javac.processorpath=\
    ${javac.classpath}
# This is used to compile .fx JDI test target apps.
# Used to define "javafxc" ant task
javafxc.jar=../dist/lib/shared/javafxc.jar
javac.classpath=\
    ${file.reference.tools.jar}:\
    ${javafxc.jar}
# Space-separated list of extra javac options
javac.compilerargs=
javac.deprecation=false
javac.source=1.5
javac.target=1.5
javac.test.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}:\
    ${javafxrt.jar}:\
    ${file.reference.junit.jar}
javadoc.additionalparam=
javadoc.author=false
javadoc.encoding=${source.encoding}
javadoc.noindex=false
javadoc.nonavbar=false
javadoc.notree=false
javadoc.private=false
javadoc.splitindex=true
javadoc.use=true
javadoc.version=false
javadoc.windowtitle=
jaxbwiz.endorsed.dirs="${netbeans.home}/../ide12/modules/ext/jaxb/api"
main.class=com.sun.javafx.tools.debug.tty.TTY
meta.inf.dir=${src.dir}/META-INF
#platform.active=JDK_1.6
platform.active=default_platform
run.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}
# Space-separated list of JVM arguments used when running the project
# (you may also define separate properties like run-sys-prop.name=value instead of -Dname=value
# or test-sys-prop.name=value to set system properties for unit tests):

# used to run JavaFX target application
javafxrt.jar=../dist/lib/shared/javafxrt.jar
# junit tests use this to spawn with proper classpath for JavaFX target apps
test-sys-prop.javafxrt.jar=${javafxrt.jar}
test-sys-prop.build.test.classes.dir=${build.test.classes.dir}
test-sys-prop.test.src.dir=${test.src.dir}
run.jvmargs=
run.test.classpath=\
    ${javac.test.classpath}:\
    ${javafxc.jar}:\
    ${build.test.classes.dir}
source.encoding=UTF-8
src.dir=src
test.src.dir=test
manifest.file=manifest.mf
