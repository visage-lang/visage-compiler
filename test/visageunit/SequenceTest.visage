/*
 * @test/visageunit
 */
 
public class SequenceTest extends visage.visageunit.VisageTestCase {
    function testSimple() {
        assertEquals([1, 2], [1, 2]);
    }

    function testSimpleMessage() {
        assertEquals("message", [3, 4], [3, 4]);
    }
}
