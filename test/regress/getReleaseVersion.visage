/*
 * @test
 * @run
 */
import java.lang.System;
import visage.lang.FX;

var version: String = FX.getProperty("visage.version");
var ok = true;
if (version == "unknown")
    ok = false;
System.out.println(if (ok) "PASS!" else "FAIL!");
	
