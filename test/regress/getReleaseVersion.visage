/*
 * @test
 * @run
 */
import java.lang.System;
import visage.lang.Visage;

var version: String = Visage.getProperty("visage.version");
var ok = true;
if (version == "unknown")
    ok = false;
System.out.println(if (ok) "PASS!" else "FAIL!");
	
